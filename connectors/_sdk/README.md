# Connector SDK
All source integrations implement one contract and must carry an approved legal_status before production enablement. Required capabilities: discovery/fetch, raw mapping, health check, rate limits, retries, deletion detection, fixtures and contract tests.
