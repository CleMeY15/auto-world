# TASK-0005A — exact local SeaweedFS candidate audit

State: second native [run 35985951236](https://github.com/CleMeY15/auto-world/actions/runs/35985951236) on protected main `63e3254bf48c4cf88e452823a8ef167cdfab5bca` **SUCCEEDED** with one `COMPLETE/DIAGNOSTIC_ONLY/NOT_AUTHORIZED` exact-candidate audit. The first run remains **FAILED** with `INCOMPLETE/REPORT_POLICY`. No private image publication or admission is claimed. TASK-0005A and TASK-0005 remain IN_PROGRESS, TASK-0006 blocked.

## Boundary and trigger

The manual `seaweed-candidate-audit.yml` workflow is confined to individually reviewed protected-main dispatches, with read-only source/artifact permission and no package or signing capability. Each reviewed workflow revision requires one exact run number and first attempt; the failed first run cannot be reclassified or rerun. It builds the corrected scanner twice from locked public materials and requires matching binary, closure and build-info evidence. It downloads the official vulnerability and Java databases by immutable manifest digest, compares registry identity before and after download, requires a vulnerability database updated within 48 hours, validates both databases' timestamps and freezes their bytes before constructing a candidate. Java database age is recorded without a maximum-age rejection under the 2026-09-24 user decision.

On 2026-09-24 the [upstream Java database workflow](https://github.com/aquasecurity/trivy-java-db/actions/workflows/cron.yml) is `disabled_inactivity`; its last successful [scheduled run 35408377819](https://github.com/aquasecurity/trivy-java-db/actions/runs/35408377819) started on 2026-09-19. Earlier scanner runs failed the then-current 48-hour Java gate. That historical failure remains failed; the revised policy permits a guarded candidate audit after review and CI, without calling Java-vulnerability coverage current. A stale vulnerability database, future/invalid timestamps, changed bytes or inconsistent manifests still fail. No metadata timestamp is edited and no private database is substituted.

## Exact-subject sequence

After database validation, the local materializer reconstructs a fresh SeaweedFS derivative from the reviewed public source and base materials. Its archive is validated against the image ID, one-layer DiffID, filesystem, configuration and Docker save identities before a callback may read it. The callback receives one bounded, short-lived path to that owned archive; the materializer removes its owned image, archive and temporary material on both success and callback failure.

The scanner reads the validated Docker save archive with `trivy image --input` in the pinned carrier. The scanner container has no network, Docker socket, capabilities, writable root filesystem or write access to the archive and frozen databases. The candidate image is never executed in this workflow. The JSON vulnerability report and CycloneDX inventory are captured from the same archive; frozen scanner, database and archive bytes are rechecked after each scan. The local policy requires exact scanner-visible archive name, Docker image ID and tag, Linux/amd64 metadata and end-of-life rejection, one `usr/bin/weed` Go executable inventory, absence of the removed Rust executable targets, complete one-to-one package/SBOM inventory and the unchanged HIGH/CRITICAL vulnerability thresholds. It accepts no vulnerability disposition.

The audit uses a private empty `DOCKER_CONFIG`, a 240-minute operation deadline inside a 270-minute execution step and a 360-minute job, with bounded earlier steps. The abort signal reaches candidate materialization. Each database or scan container has a run-scoped name and random ownership nonce; the runner checks for a collision, inspects exact image/name/labels before any forced removal, then proves absence. A timeout, ambiguous ownership or failed cleanup makes the receipt `INCOMPLETE`. The runner retains its fixed audit failure code after the materializer has disposed the candidate; an image or temporary cleanup failure takes precedence.

The bounded public receipt binds run/revision, source and recipe, image ID, DiffID, archive/config/layer identities, scanner build identity, both database manifests and frozen files, report hashes, finding/blocker counts and cleanup. A complete report with blockers is `BLOCKED` and fails the job after cleanup. A command, freshness, identity, report, policy or cleanup error is `INCOMPLETE`; it is never recorded as a clean scan. Public Actions artifacts may retain only the technical scanner builds, JSON/SBOM reports and bounded receipts, never the candidate archive or private image layers.

## First native run and focused repair

[Run 35982152683](https://github.com/CleMeY15/auto-world/actions/runs/35982152683), first attempt on exact protected main `923b404e284ae73420cf6266fc71a04d9c92a612`, passed both independent scanner builds, official database capture, archive materialization and JSON/CycloneDX scans, but its receipt ended `INCOMPLETE/REPORT_POLICY/seaweed_candidate_audit_invalid`. The vulnerability database was 2,413,112 ms old against the 172,800,000 ms limit; Java was 463,729,032 ms old and recorded `freshAt48Hours: false`. The subject was one 246,525,440-byte saved archive with SHA-256 `c41245121f2e5f79ea9c8a0fbb16573ec7d1b144a75397b7e1d3f66c8b38f08e`, image ID `sha256:30852e096f1f426012c3ca6c1e44f6adb9acd00aec0225edc00ec78d774288f9` and DiffID `sha256:14383f2ea938d9fb54669caeedd774623b93327747c350f7760f7d2c1602fa95`. Four owned scanner containers were proven absent and the final temporary-directory cleanup step passed. No private package or image was published.

The retained vulnerability JSON (SHA-256 `91504b3a55a43669aeaa391092fddc947631af525a72fd037cb02f20ee90c08e`) shows 284 package identities, matching 284 libraries in the CycloneDX SBOM (SHA-256 `ad14162ed17da1105b5e040e0f5f284369e2be3af16c324d6ad7195424381e95`), with no HIGH/CRITICAL finding in that JSON report. They do **not** constitute an accepted clean audit: the local validator expected an explicit `EOSL` boolean although the scanner omitted it, accepted CycloneDX only through 1.6 although the native SBOM is 1.7, and rejected the SBOM's single Alpine `operating-system` component. A local diagnostic replay reached `COMPLETE` only after changing those three fields in memory; no historical artifact was edited. [PR80](https://github.com/CleMeY15/auto-world/pull/80) repaired those narrow native-shape assumptions with matching OS/package/end-of-life guards and negative tests, passed [exact-head quality 35985530727](https://github.com/CleMeY15/auto-world/actions/runs/35985530727) and [main quality 35985794558](https://github.com/CleMeY15/auto-world/actions/runs/35985794558), then authorized only run number 2 attempt 1. Run 1 remains failed.

The pinned Trivy source encodes `EOSL` as an [omitted false boolean](https://github.com/aquasecurity/trivy/blob/e1fd17a0ea4a8cf24bc4b4dd7e2cfbf4bb31b994/pkg/fanal/types/artifact.go#L24-L34), emits [CycloneDX 1.7](https://github.com/aquasecurity/trivy/blob/e1fd17a0ea4a8cf24bc4b4dd7e2cfbf4bb31b994/pkg/sbom/cyclonedx/marshal_test.go#L328-L332), and models an [OS package result as an operating-system component](https://github.com/aquasecurity/trivy/blob/e1fd17a0ea4a8cf24bc4b4dd7e2cfbf4bb31b994/pkg/sbom/io/encode.go#L195-L218). An omitted `EOSL` says only that this scanner execution did not mark the OS end-of-life; the corrected validator also requires an exact OS package result and matching OS component. It does not independently certify Alpine support status.

## Second native run — exact local audit complete

[Run 35985951236](https://github.com/CleMeY15/auto-world/actions/runs/35985951236), number 2 attempt 1, was dispatched manually on exact protected main `63e3254bf48c4cf88e452823a8ef167cdfab5bca`. Both independent scanner builds succeeded with the same 168,288,382-byte binary SHA-256 `1255e0feaf879d9b34fa7b8842e4b3dca53429171b03b2576fafdee218a59e3d`. The native audit job and its separate owned temporary-directory cleanup both passed. The downloaded `audit-receipt.json` (5,791 bytes, SHA-256 `e65fd0254d671fda01200a29f5f7bbcda0f0f1a2001449035b0be4ff85a3df2a`) records `SEAWEED_EXACT_CANDIDATE_AUDIT_V1`, `COMPLETE`, `DIAGNOSTIC_ONLY`, `NOT_AUTHORIZED`, zero findings and zero blockers; publication, admission and image execution are `NOT_ATTEMPTED`.

The official vulnerability database was 4,625,518 ms old, below its 172,800,000 ms maximum, with frozen database SHA-256 `3cff07389d081edc56b1696fafa7393a8cdbf355d1160500577201a3053575b8` and registry manifest `sha256:ca05ccdc370a86e05e6256005d0cfcd40641253b25ae4a30522b1dcc6801611f`. The Java database was 465,941,438 ms old, `freshAt48Hours:false`, with frozen SHA-256 `1fce16f732df52f325d9922f5a8ff7221719a7819cdb5ba1dc76fe32b4a36e4c` and manifest `sha256:d83c6dd4d525be30ffecc92e5be679392be56547c29cf70f76a7478b08981c9c`; its age is recorded but has no maximum-age rejection. Both metadata timestamps were validated as ordered and non-future and both registry identities and frozen bytes were checked before and after scanning. This result does not establish current Java-vulnerability coverage.

The 246,525,440-byte saved archive had SHA-256 `d96b551cea079d48df2b58502d3fe30752f7df7da6ec4d634789720664e9ef33`, image ID `sha256:eb9416cf9665788e37947e2b191eba6b5780f88ab1e6637a1a2f927c166cbc7d` and DiffID `sha256:14383f2ea938d9fb54669caeedd774623b93327747c350f7760f7d2c1602fa95`. The retained vulnerability JSON (174,527 bytes, SHA-256 `5f1a1c1863a124b26bec36193ee37ac478822716f76d65ec7a5c012f11ecf12a`) has zero vulnerability findings. Its 284 package identities match 284 CycloneDX libraries, alongside one Alpine operating-system and one application component in the 1.7 SBOM (263,867 bytes, SHA-256 `e67e5efbbfa3f63d91548bc4d52b25fb2dc242eb82aaf6653e9f046aa0c900d4`). The receipt proves all four owned database/scan containers absent, and the runner's final cleanup step passed. The disposable archive and image were not retained or published.

## Acceptance and remaining limits

Each workflow revision requires targeted negative tests, full lint/typecheck/build/tests, independent infrastructure/security review, exact-head CI and protected-main CI before its guarded native run. The orchestration tests cover stale vulnerability-database refusal before materialization, complete and blocked reports, archive mutation and cleanup; they use injected commands and are not a native scanner result. Run 2 supplies the actual `--input` report metadata, complete inventory, immutable database identities and ages, cleanup and final blocker status. This success proves one exact-subject local audit under the revised age policy, not current Java-vulnerability coverage. Private publication, official attestation, retention/restore, admission and the four-service data lifecycle remain separate ADR-0007/TASK-0005 gates. The user-skipped external fork access probe remains `SKIPPED_BY_USER` and is not restored by this workflow.

Rollback disables or removes this manual diagnostic while preserving its historical receipts. It does not change any admitted image, service data or production traffic.
