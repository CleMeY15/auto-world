# TASK-0005A — Authenticated raw source ZIP download

Status: raw-byte acquisition VERIFIED by the one-time native diagnostic. No ZIP content has been admitted, extracted or used to construct an image.

## Boundary

[PR43](https://github.com/CleMeY15/auto-world/pull/43) merged the live GitHub source-origin gate after independent review and final-head/protected-main CI. This increment consumes that gate internally and downloads the exact five ZIP artifacts from the reviewed run by fixed artifact ID. The caller supplies only an empty, absolute, user-owned `0700` directory, an optional abort signal and an aggregate deadline. It cannot choose a repository, URL, artifact, digest, executable or subprocess.

The Linux runner opens and retains the directory descriptor, creates five fixed-name direct children with `O_CREAT|O_EXCL|O_NOFOLLOW|O_RDWR` and mode `0600`, and accesses them through `/proc/self/fd/<directory-fd>/`. Each `gh api` child uses GET against `github.com` and the pinned REST version, has no shell or writable stdin, and must finish before the next artifact starts. The transfer rejects an early end, extra byte, bad SHA-256, nonzero exit, abort or timeout. It then syncs and rehashes the saved file through the retained descriptor, checking that the owned regular file and directory identities still match their names. A second complete persisted-byte reread precedes the fresh independent GitHub origin check, which rejects deletion, rerun, expiry or changed metadata during the download. The deadline and abort signal apply during every reread; the final origin is required again immediately before success. A failure returns no receipt and removes only created files whose inode identities still match; it never recursively clears a caller directory or deletes an unknown replacement. If a path changes or cleanup cannot prove absence, the operation reports `seaweed_raw_zip_cleanup_failed` and leaves the uncertain path for manual inspection.

The result is explicitly `RAW_ZIPS_ONLY`, `PREPARATION_ONLY`, `zipValidation: NOT_RUN`, `extraction: NOT_RUN` and `candidateAuthorization: NOT_AUTHORIZED`. Its JSON fields do not carry the private GitHub-origin mark. The next structural scan is a separate read-only preparation gate. A later materialization transaction must freshly revalidate the owned files, scan all five ZIP structures and contents, check the two build trees and three receipts, and only then consider an atomic promotion. The returned paths are not durable authority against a later writer. The trusted workflow-owned parent and same-UID/privileged-writer assumption match the bounded ZIP reader's filesystem contract.

## Operational evidence and next gate

The one-time [native diagnostic 35911097961](https://github.com/CleMeY15/auto-world/actions/runs/35911097961) on protected main `7a23fc09de3e8916f902d29302242539521c2830` **PASSED**. The intake returned `RAW_ZIPS_ONLY` for the five exact reviewed artifact IDs after a fresh origin reread. Independent shell checks confirmed all five saved byte sizes, owner `1001`, mode `0600`, link count one and exact SHA-256 values; the cleanup step reported `CLEANED`. [Final-head CI 35910570604](https://github.com/CleMeY15/auto-world/actions/runs/35910570604) and [main CI 35910871704](https://github.com/CleMeY15/auto-world/actions/runs/35910871704) passed 305 Linux root tests without skips, plus lint, typecheck, nine builds, secret scan and dependency audit. This is raw transport evidence only; the temporary ZIPs were removed and no extraction or candidate authorization occurred.

The source artifacts expire on 7 October 2026. The one-time main diagnostic has already consumed its run-number-1/attempt-1 budget and its five temporary ZIPs were removed. A later retry requires a separately reviewed workflow change. The next [authenticated structural ZIP scan](TASK-0005A-SEAWEED-ZIP-STRUCTURAL.md) must obtain a new fresh source view and re-download the five artifacts; it cannot reuse the serialized raw-byte receipt as authority. No production credential, package permission, registry write, Docker operation or data volume changed.

Rollback removes this preparation module, diagnostic entrypoint/tests and document. TASK-0005A and TASK-0005 remain IN_PROGRESS; TASK-0006 remains blocked. Exact-subject image audits, private-package retrieval and the four-service lifecycle remain separate gates. Under the 2026-09-24 policy amendment, the vulnerability database still needs an update within 48 hours, while Java database age is recorded without blocking solely on age; older Java data limits current Java-vulnerability coverage. The user-waived external fork-access test remains `SKIPPED_BY_USER`.
