# TASK-0005 validation — not accepted

Status: IN_PROGRESS. No Done, merge or TASK-0006 authorization. [Draft PR #7](https://github.com/CleMeY15/auto-world/pull/7) contains the implementation. Real-container integration and quality passed on `932ad80`; the exact-image audit and required final independent review remain open.

## Scope and prerequisite

TASK-0004 merged at `b9d22a2123ff53acded73eaf800a29dc8f2faf66`, with passing [main CI](https://github.com/CleMeY15/auto-world/actions/runs/34031090389). ADR-0004 was committed before implementation after separate Architect and Critic plan reviews. This task is a local/CI storage foundation, not connector execution, a production SDK store, a real data source, a deployed platform or a user-facing UI.

## Current evidence

- Pinned Node22.23.2/pnpm10.15.0/Turbo2.10.12; local forced `pnpm check` passed on the implementation worktree including restore-negative/candidate-audit additions. Lint9, typecheck11, test18 and build9 Turbo tasks execute without cache; secretlint and dependency audit pass. This does not prove container behavior.
- [CI at 5533610](https://github.com/CleMeY15/auto-world/actions/runs/34035880355): quality passed, integration and image audit failed. Service configuration ownership was repaired without loosening host credential permissions.
- [CI at 3c70094](https://github.com/CleMeY15/auto-world/actions/runs/34036344679): quality passed; PostgreSQL/Redis usable probes passed, OpenSearch/S3 usable readiness failed even though Docker healthchecks were healthy. Therefore process health is not accepted as API-readiness evidence.
- [CI at 6a1c0b2](https://github.com/CleMeY15/auto-world/actions/runs/34036687560) adds fixed-code readiness diagnostics, synthetic incompatible/tampered/existing restore-target cases, and additional candidate-image scans. Quality passed; candidate-image audit and real integration failed.
- [CI at 4c25d34](https://github.com/CleMeY15/auto-world/actions/runs/34036970100) preserved the first failure: OpenSearch's published-port lookup failed; later S3 timeouts were deadline exhaustion while waiting. Independent Architecture approved correcting the internal-only network contradiction. ADR amendment `f637667` preceded the non-internal project-bridge implementation and four host protocol probes at `43c1f9d`. Its [CI](https://github.com/CleMeY15/auto-world/actions/runs/34037568636) passed host protocols, usable startup, migrations, atomic raw/outbox and concurrent writes; a negative membership fixture hit a duplicate primary key before its intended foreign-key check.
- [CI at 8d4ccd9](https://github.com/CleMeY15/auto-world/actions/runs/34037895575) passed quality and the corrected relational/privilege cases, pending-outbox index, restart persistence, all four dependency failure/recovery cases, and refusal of incompatible/tampered/existing restore targets. Cold backup recovered the source's mixed running/stopped state, but the isolated positive restore failed with `restore_target_nonempty`. Full restore, injected verification failure cleanup and sentinel reset are not yet proven by this run. The image audit also failed; neither failure is waived.
- A fresh HTTPS clone at `6a1c0b231b512d1ce410a4874be49484f14b096b` passed frozen install and forced `pnpm check` (exit0, clean Git status): root50 tests,49 passed and one POSIX-only signal test skipped on Windows; Vehicle99, Registry77, SDK157 passed; lint9/typecheck11/test18/build9 Turbo tasks, zero cached; secretlint159 files and dependency audit passed. No working-tree files, build outputs or Git object alternates were copied; only the pnpm download store was reused. A later implementation SHA still needs fresh acceptance evidence.
- On resumption, a second fresh HTTPS clone at `b233d0a46a3ee6ba4d6075e5bf6602dec28bc21a` passed frozen installation and forced root checks (exit0, clean Git status): root60 tests,57 passed/3 POSIX-only Windows skips; Vehicle99/Registry77/SDK157; lint9/typecheck11/test18/build9, zero cached, secretlint164 and dependency audit passed. The same SHA's Linux quality job passed in [CI34142987078](https://github.com/CleMeY15/auto-world/actions/runs/34142987078). This checkpoint adds safe service-specific restore refusal diagnostics, not a restoration fix or task acceptance.
- CI34142987078 reproduced the runtime progress and identified exactly `restore_target_nonempty_object-store`. Upstream SeaweedFS4.45 precreates `/data/filerldb2`; Docker copy-up therefore contradicts the empty-target restore check. Independent Architecture approved a single raw-volume `nocopy` setting, with the existing upstream ownership/drop-privileges entrypoint and all four empty-volume guards retained. ADR amendment `ef1c1e4` precedes the fix. The new static regression failed before the change; real startup/restore tests additionally check PID1 UID/GID1000 and raw/filer directory ownership.
- Corrective implementation `932ad8024689970fd2b8278014b617c88c19a4bb` passed13/13 targeted tests and forced root checks in both the working checkout and another fresh HTTPS clone (frozen installation, exit0, clean Git): root61 tests/58 passed/3 POSIX-only Windows skips, Vehicle99/Registry77/SDK157, lint9/typecheck11/test18/build9 with zero cached tasks, secrets/dependency audit passed. Both Linux quality and real integration passed in [CI34143901867](https://github.com/CleMeY15/auto-world/actions/runs/34143901867); the image audit remains failed.

### Complete real-container evidence at 932ad80

The sanitized `data-integration` artifact records `status:passed` at `2026-09-07T16:45:58Z`. All17 named phases passed: exact configuration/pulls, four loopback protocols, non-root raw ownership, fresh usable startup, transactional/replayed/concurrent migrations, atomic canonical/outbox commit and abort, conditional S3 writes and real races, negative relational/privilege cases, pending index, restart persistence, each dependency's failure/recovery, incompatible/tampered/existing restore refusals, mixed-state cold backup and isolated restore, and scoped reset preserving an independent sentinel.

The positive restore proves canonical snapshot equality, raw SHA256 retrieval and restored raw service UID/GID1000. The same phase injects a verification failure, proves temporary target credentials are removed on both outcomes, and rechecks original data and the originally stopped Redis state. The outer backup/restore phase took239858ms across separately bounded archive/recovery/restore/cleanup operations; total integration stayed within its20-minute orchestration budget. Artifact retention is14 days; rerun after expiry or any implementation change. These are local/CI single-node proofs, not production crash consistency or an atomic distributed SDK store.

## Independent findings and corrections

A preliminary independent code/security review of `daa20b17749577a6ba0b699f65c8c38b62929995` requested changes: raw upload/download scratch survived, cooperative signal cancellation could strand stopped services, and restored test credentials survived reset. The implementation adds guarded scratch cleanup, signal-aware subprocess cancellation with separately bounded recovery, and verified deletion of newly generated test-project credentials only after resources are absent. Native tests cover normal/error/cancellation behavior; POSIX signal delivery runs on Linux and is skipped on Windows. Independent re-review and complete real recovery evidence are still required; these changes are not self-approved completion.

The second safety review at `6a1c0b2` cleared raw scratch, core mixed-state recovery and existing-target preservation, but found subprocess-closure ordering, partial initialization and synthetic backup-copy cleanup gaps. Commit `36651bc` adds closure-before-rejection with bounded termination, stable helper absence checks, signal exit identity, transactional cleanup of each secret-bearing initialization step, and protected deletion of negative-test copies. Targeted tests at that point:45 total,44 passed, one POSIX-only skip on Windows. A real child PID is proven absent before a recovery marker is written. Independent narrow review at `43c1f9d` cleared these three gaps and identified a CLI health exit-code overwrite. The fix received narrow independent CLEAR at exact SHA `8d4ccd93bbb1e3a6c7bb5f04137c758336d670a3`; POSIX signal tests passed in Linux quality CI. These bounded clearances are not final whole-task code/security or architecture approval.

A further bounded independent backup/runtime/ephemeral security review at exact `b233d0a` found no new actionable issue outside the known restore error; its targeted tests passed23/23. This does not approve the image set or unproven restore path.

The copy-up correction received independent Architect CLEAR on exact `932ad8024689970fd2b8278014b617c88c19a4bb`, conditional on real restore/startup evidence. A separate code-reviewer launch failed with `agent thread limit reached`. That required independent lane is unavailable; neither author checks nor the Architect lane substitute for it. There is no whole-task merge-ready review.

## Image audit blocker

### Incremental audit-policy hardening (branch `codex/task-0005-audit-binding`)

The Trivy report gate now binds `ArtifactName` to the pinned repository and either
the manifest or Linux/amd64 platform digest. A report carrying a valid digest under
another repository, or a repository with an unrelated digest, is rejected before
findings are evaluated. This closes report-substitution ambiguity; it does not
change any image pin, waive a vulnerability, or make the current audit green.

The regression is covered by `tests/data-infra-audit.test.mjs` and passes in the
targeted suite. The task remains `IN_PROGRESS`: the complete shipped-image audit
still blocks acceptance until compliant pins or a separately reviewed supply-chain
decision exists.

The [complete image audit](https://github.com/CleMeY15/auto-world/actions/runs/34035149757) identified the following findings on the exact original ADR-0004 pins. Complete reports and scanner/database metadata are retained as the `data-image-audit` CI artifact (14-day retention); rerun before relying on expired evidence.

| Image | HIGH/CRITICAL findings | Blocking findings |
| --- | ---: | ---: |
| PostgreSQL17.11 Debian | 111 | 111 |
| OpenSearch3.8.0 | 52 | 52 |
| Redis8.4.6 Debian | 55 | 55 |
| SeaweedFS4.45 | 2 | 2 |
| AWS CLI2.36.40 | 0 | 0 |
| Trivy0.74.0 | 3 | 3 |

Every CRITICAL and fixable HIGH remains blocking. Unfixed HIGH also blocks without an exact, dated, independently reviewed disposition; none is granted. The scanner itself is not excluded. Official Alpine PostgreSQL/Redis candidates are recorded separately in `infra/image-candidates.json`; scanning a candidate does not adopt it or remove any shipped subject.

The candidate audit at `6a1c0b2` also failed: PostgreSQL17.11-alpine3.24 has31 blocking findings (one CRITICAL Go stdlib and30 fixable HIGH); Redis8.4.6-alpine3.22 has8 fixable HIGH in OpenSSL/setpriv. Neither is adopted. Their smaller finding counts are not evidence of acceptance. The same run still reports all six original shipped images.

The complete eight-subject audit at `b233d0a` on 2026-09-07 reproduced these same counts with fresh scanner/database evidence in CI34142987078. No baseline image was omitted, and neither candidate was adopted.

Independent Architecture review: **BLOCK completion** under the current image set. Candidate-only Alpine audits are approved. A temporary upstream SeaweedFS dev pin requires verified official signature, a narrow dated ADR, full audit and S3/recovery validation. No conforming official OpenSearch/Trivy replacement has yet been evidenced. Creating Auto World-owned patched images would materially expand build, provenance, signing, distribution and maintenance responsibilities and requires explicit direction plus a separately reviewed supply-chain ADR. No threshold reduction is authorized.

## Remaining acceptance gates

1. Preserve the complete real Linux integration proof at `932ad80`; rerun it after any image or implementation change.
2. Pass unchanged exact-image audits; review and revalidate any adopted replacements.
3. Obtain independent final infra/code/security APPROVE and Architecture CLEAR on the same implementation SHA.
4. Refresh complete root/fresh-clone evidence, commit acceptance/DONE only after all gates pass, verify final-head CI, then merge and verify main.

The Windows host lacks Docker/Podman; this is a local prerequisite gap, not grounds to waive Linux CI. No production access right, UI polish, performance scale or fault-proof distributed persistence is claimed.
