# TASK-0003 validation

Status: IN_PROGRESS — no completion or production authorization claim.

## Scope and prerequisites

TASK-0002 is merged at `fe82aaf`; [main CI](https://github.com/CleMeY15/auto-world/actions/runs/34018855938) passed. TASK-0003 is the singular READY frontier. [PR #5](https://github.com/CleMeY15/auto-world/pull/5) carries the implementation plan; ADR-0002 was committed at `9c22a3e` before code after sequential independent Architect and Critic plan approval.

The change is an internal declared-policy/audit/health contract with one workspace dependency and an additive public SourceId parser. It does not approve a real source, authenticate a registry operator, execute retention/takedown, start transport, deploy infrastructure or add UI. Structural eligibility is not operational authority.

## Verification protocol

Use Node 22.23.2, pnpm 10.15.0 and Turbo 2.10.12. Run package lint/typecheck/tests/build, root `pnpm check` (including secrets/dependency audit), `git diff --check`, independent code/security and architecture review, then a fresh HTTPS remote clone/frozen install/direct package test from absent dist and full check. No copied working-tree/build/node_modules files or Git object alternates. Final-head CI and merged-main verification are distinct gates.

## Evidence collected so far

- TDD RED: root contracts failed for missing ninth package and missing dependency-build ordering; the new public SourceId test failed because the export did not exist. After implementation the root 10 tests and vehicle 91 runtime tests/type fixtures pass. The previous 88 vehicle regressions remain intact.
- Independent registry test author captured public-export `ERR_MODULE_NOT_FOUND` before package implementation. The final local package suite has **75 runtime tests**, with type fixtures and a full synthetic lifecycle through JSON serialization. No skipped/todo tests or coverage percentage are claimed.
- Frozen install passes. The lockfile adds only `source-registry -> vehicle-schema` as `workspace:*`; no external version or dependency changes.
- Turbo typecheck dry-run contains 10 tasks: nine package checks plus vehicle-schema build. Source-registry typecheck explicitly waits for vehicle-schema build and typecheck; direct package scripts also rebuild the public dependency.
- Root `TURBO_FORCE=true pnpm check` passes with zero cached tasks: lint 9 tasks, typecheck 10 tasks, build/test 18 tasks, build 9 tasks. Root 10 tests, vehicle 91 tests, registry 75 tests and seven placeholder build-boundary smoke checks pass. Secretlint passes for 103 repository files; dependency audit reports no known vulnerabilities. `git diff --check` is clean.
- Regression corrections preserve public `$.nextRevision` error paths, identify offending metrics in zero-count samples, and classify missing measurements as unknown even at zero volume. Type fixtures isolate readonly errors from possibly-undefined array access and compile an actual narrowed public `parseSourceId` consumer. Eligibility returns the full declared policy and never an operational `allowed` capability.
- Source/test authors completed disjoint lanes and targeted lint/typecheck/test/build. Independent implementation review, fresh remote clone and CI remain pending; ADR-only CI is not implementation proof.

## Acceptance and operational boundaries

The final evidence must prove all seven legal statuses, every enable precondition, scoped-grant anti-combination behavior, VIN/PII privacy, secret-only references, history/replay/takedown/expiry, immutable detached data, sanitized hostile inputs, deterministic health, type brands/readonly fields and built-public-export synthetic lifecycle. Health must never confer rights. No skipped/todo or happy-path-only suite substitutes for these proofs.

Validation/eligibility/health codes are the future observability boundary; no new backend workflow exists to instrument. UI/a11y/visual/customer analytics gates are inapplicable to this internal package. ADR-0002 documents trust prerequisites and rollback/versioning: additive V1 before consumers, coordinated readers/writers afterward, never destructive history rewrites.
