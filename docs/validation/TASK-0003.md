# TASK-0003 validation

Status: VALIDATED — implementation accepted; integration is gated by the final PR HEAD checks. No production source authorization is claimed.

## Scope and prerequisites

TASK-0002 is merged at `fe82aaf`; [main CI](https://github.com/CleMeY15/auto-world/actions/runs/34018855938) passed. TASK-0003 is the singular READY frontier. [PR #5](https://github.com/CleMeY15/auto-world/pull/5) carries the implementation plan; ADR-0002 was committed at `9c22a3e` before code after sequential independent Architect and Critic plan approval.

The change is an internal declared-policy/audit/health contract with one workspace dependency and an additive public SourceId parser. It does not approve a real source, authenticate a registry operator, execute retention/takedown, start transport, deploy infrastructure or add UI. Structural eligibility is not operational authority.

## Verification protocol

Use Node 22.23.2, pnpm 10.15.0 and Turbo 2.10.12. Run package lint/typecheck/tests/build, root `pnpm check` (including secrets/dependency audit), `git diff --check`, independent code/security and architecture review, then a fresh HTTPS remote clone/frozen install/direct package test from absent dist and full check. No copied working-tree/build/node_modules files or Git object alternates. Final-head CI and merged-main verification are distinct gates.

## Execution evidence

- TDD RED: root contracts failed for missing ninth package and missing dependency-build ordering; the new public SourceId test failed because the export did not exist. After implementation the root 10 tests and vehicle 91 runtime tests/type fixtures pass. The previous 88 vehicle regressions remain intact.
- Independent registry test author captured public-export `ERR_MODULE_NOT_FOUND` before package implementation. The accepted package suite has **77 runtime tests**, with type fixtures and a full synthetic lifecycle through JSON serialization. No skipped/todo tests or coverage percentage are claimed.
- Frozen install passes. The lockfile adds only `source-registry -> vehicle-schema` as `workspace:*`; no external version or dependency changes.
- Turbo typecheck dry-run contains 10 tasks: nine package checks plus vehicle-schema build. Source-registry typecheck explicitly waits for vehicle-schema build and typecheck; direct package scripts also rebuild the public dependency.
- Accepted implementation: `cdb1187a2f3ec28ec4be3de1a8caac5f2c18d53c`. Root `TURBO_FORCE=true pnpm check` passes with zero cached tasks: lint 9 tasks, typecheck 10 tasks, build/test 18 tasks, build 9 tasks. Root 10 tests, vehicle 91 tests, registry 77 tests and seven placeholder build-boundary smoke checks pass. Secretlint passes for 103 repository files; dependency audit reports no known vulnerabilities. `git diff --check` is clean.
- Regression corrections preserve public `$.nextRevision` error paths, identify offending metrics in zero-count samples, and classify missing measurements as unknown even at zero volume. Type fixtures isolate readonly errors from possibly-undefined array access and compile an actual narrowed public `parseSourceId` consumer. Eligibility returns the full declared policy and never an operational `allowed` capability.
- Source/test authors completed disjoint lanes and targeted lint/typecheck/test/build. Independent native code/spec/security review: **APPROVE**, zero remaining findings, at `cdb1187`; independent native architecture review: **CLEAR**, same SHA. These are separate automated specialist reviews, not external human or legal approval. The reviewer independently reran registry lint/typecheck/build/77 tests; the architect reran the 14 history tests.
- Both reviews caught an audit-integrity defect at the earlier `46254a5`: unchanged `replace_configuration` could disguise a disable or create a no-op. The independent test author captured exactly two RED cases, then 14/14 history and 77/77 full tests GREEN after the guard and ADR clarification. Original snapshots and explicit event semantics are preserved.
- Two fresh HTTPS remote clones were checked, including a new corrected-head clone at `cdb1187`. Each began without node_modules, either package's dist or Git object alternates; frozen install, direct registry tests from absent dist, then forced root `pnpm check` passed. No working-tree files, node_modules or build outputs were copied. Git status/diff checks were clean. Reusing pnpm's verified download store does not reuse build artifacts.
- Initial implementation [CI at 46254a5](https://github.com/CleMeY15/auto-world/actions/runs/34024080659) and corrected implementation [CI at cdb1187](https://github.com/CleMeY15/auto-world/actions/runs/34024356587) passed. ADR-only CI is not used as implementation evidence.
- This same-branch evidence/DONE commit must pass [PR #5 final checks](https://github.com/CleMeY15/auto-world/pull/5/checks) before merge. The PR integration comment records exact final HEAD, CI and merge/main result. A passing earlier commit does not substitute for the final-head gate; TASK-0004 cannot start before integration and main verification.

## Acceptance and operational boundaries

| Acceptance contract | Concrete proof |
|---|---|
| All seven legal statuses, complete policy and method compatibility | `registry.test.mjs`, `policy.test.mjs`: status/required-key tables, grant/retention/cache/media/PII/takedown and authorization bounds |
| No Cartesian rights or public VIN/PII | `policy.test.mjs`: territory/method/audience clauses, no field union, consumer/B2B restrictions |
| Audit/history/expiry/takedown and exact replay | `history.test.mjs`, `lifecycle.test.mjs`: truthful transitions, changed replacement, immutable prior snapshots, latest-only replay, re-enable/expiry/takedown serialization |
| Health never grants rights or invents missing data | `health.test.mjs`: windows, null/zero/rate/age boundaries, deterministic precedence and healthy blocked source |
| Strict safe ownership and sanitized errors | `hostile.test.mjs`, `registry.test.mjs`: unknown keys, prototypes, descriptors, arrays, Proxies, caller detachment and freezing |
| Cross-package IDs and reproducibility | type fixtures, public SourceId regression tests, built self-exports, root contracts, fresh corrected clone and CI |

No real licence, authenticated registry/CAS, source transport, retention execution, dashboard or production end-to-end workflow is proven by these synthetic contract tests.

Validation/eligibility/health codes are the future observability boundary; no new backend workflow exists to instrument. UI/a11y/visual/customer analytics gates are inapplicable to this internal package. ADR-0002 documents trust prerequisites and rollback/versioning: additive V1 before consumers, coordinated readers/writers afterward, never destructive history rewrites.
