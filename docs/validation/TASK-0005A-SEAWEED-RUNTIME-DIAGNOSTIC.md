# TASK-0005A — Isolated SeaweedFS runtime diagnostic

Status: the third native runtime run verified its disposable candidate and cleaned up; image admission remains NOT_AUTHORIZED. TASK-0005A and TASK-0005 remain IN_PROGRESS, and TASK-0006 remains blocked.

## Boundary

The existing [local candidate proof](TASK-0005A-SEAWEED-LOCAL-CANDIDATE.md) authenticates construction and export, then disposes the image. This separate, one-time diagnostic reconstructs the candidate from the same reviewed source and base inputs, verifies its saved archive, and starts **that run's newly owned image ID** before disposal. The historical construction diagnostic retains `imageExecution: NOT_ATTEMPTED`; neither its run-3 image ID nor its archive is reused.

The runtime starts only on a fresh GitHub-hosted Ubuntu runner under Docker 28.0.4, on protected main, the individually reviewed workflow run and first attempt. Repository and Actions permissions are read-only. No port is published, no image or evidence archive is uploaded, and no registry write occurs. The container has no network except loopback, a read-only root filesystem, UID/GID `1000:1000`, no Linux capabilities, `no-new-privileges`, private bounded writable `/tmp` and `/data` tmpfs mounts, and the existing 768 MiB, 0.75 CPU, 512 PID and 30-second stop limits. The synthetic S3 configuration is restricted to this disposable diagnostic and must be mode `0600`.

The fixed command is ADR-0008's Go server/S3 profile. The diagnostic checks the derivative version, PID-1 identity, private `/tmp` mode, internal master readiness on 9333, filer readiness on `8888/readyz`, S3 readiness on `8333/readyz`, an exact HTTP 403 for unsigned `GET /` on 8333, absence of Iceberg/Lance listeners on 8181/9101, absence and failed direct invocation of both unsupported Rust helpers, and bounded shutdown. The container, runtime configuration, image, saved archive, rootfs and temporary directories must be cleaned before a success receipt. A random per-run label and image-ID inspection recover ownership even if Docker creates a container but loses its command response; a foreign or uncertain container is preserved and blocks success. Public diagnostic output contains only fixed phase/result/reason, bounded duration, run/revision identifiers and an image hash when known; it never includes raw Docker output or the synthetic credential. A failure or uncertain cleanup does not authorize a retry that bypasses a control.

Any successful receipt is `SEAWEED_LOCAL_RUNTIME_CANDIDATE_RECEIPT_V1/VERIFIED/DIAGNOSTIC_ONLY/NOT_AUTHORIZED`, with `imageExecution: VERIFIED_DIAGNOSTIC` and publication, vulnerability audit and admission each `NOT_ATTEMPTED`. It is evidence only for the image ID reconstructed during that run. The exact candidate digest must be retested before supported use.

## Remaining acceptance

This diagnostic cannot prove host-loopback publication while Docker networking is disabled. It does not prove valid signed S3 operations, forbidden scope, conditional or concurrent writes, persistence, restart, backup or restore. Those ADR-0008 controls and fresh complete SBOM/vulnerability audits remain prerequisites for private admission. The scanner's Java database freshness gate is unresolved. The external fork access test is `SKIPPED_BY_USER` and its boundary remains `NOT_VERIFIED`.

## Native diagnostic history

[PR57](https://github.com/CleMeY15/auto-world/pull/57) merged the isolated runtime diagnostic at `1f3808fd61ea8d87a4ff47ee49b3f1f0f979a303` from final head `577cc41c881492f1e6731db0871986bdb0f2d21e`. Its [exact-head CI](https://github.com/CleMeY15/auto-world/actions/runs/35943824693) and [protected-main CI](https://github.com/CleMeY15/auto-world/actions/runs/35944146992) passed all gates; the Linux quality run passed 479 root tests without skips. Internal independent review found no blocking code issue after repairs; its formal review was limited because `lsp_diagnostics` was unavailable.

The first [native runtime run 35944294445](https://github.com/CleMeY15/auto-world/actions/runs/35944294445), run 1 attempt 1 on that main, **FAILED** at `RUNTIME_PROBE/ANONYMOUS_NOT_REFUSED` after constructing a new candidate image `sha256:3af49fdc40cea36cfb06b36f9cc9abcdcdf03adf7961b07b19d64b9ed62782ed`. The public result did not distinguish a temporarily unavailable S3 listener (`curl` HTTP `000`) from an actual non-refusal such as HTTP `200`. The separate temporary-directory cleanup reported `CLEANED`; the runtime transaction did not report a container-cleanup failure. This run produced no verified runtime receipt and authorized no image.

The focused PR58 repair polls SeaweedFS's unauthenticated `/readyz` before the unsigned request, then accepts **only** HTTP `403`. It fails separately for S3 unavailable, HTTP `200` and any other status. The pinned upstream source documents [the ready endpoint](https://github.com/seaweedfs/seaweedfs/blob/c5073360007d28385a33426a42ac3e4ec504c5a3/weed/s3api/s3api_server.go#L714-L717) and [configured authentication](https://github.com/seaweedfs/seaweedfs/blob/c5073360007d28385a33426a42ac3e4ec504c5a3/weed/s3api/auth_credentials.go#L336-L389).

[PR58](https://github.com/CleMeY15/auto-world/pull/58) merged the readiness repair at `c45225013be19a6af0213a2587776189a3655613` from final head `9514b0422bf5dfc8148f8639d6f148cc38160bde`; [exact-head CI 35945167204](https://github.com/CleMeY15/auto-world/actions/runs/35945167204) and [protected-main CI 35945304966](https://github.com/CleMeY15/auto-world/actions/runs/35945304966) passed all gates. Independent code review found no actionable issue, with formal review limited by unavailable LSP diagnostics. The second [native runtime run 35945479803](https://github.com/CleMeY15/auto-world/actions/runs/35945479803), run 2 attempt 1 on that main, **FAILED** at `RUNTIME_PROBE/S3_UNAVAILABLE` after 273,705 ms of bounded runtime diagnosis. The separate temporary cleanup reported `CLEANED`; no runtime receipt or image admission followed.

Pinned source analysis identified a concrete runtime-profile defect: `--read-only` left `/tmp` unwritable, while [server registration](https://github.com/seaweedfs/seaweedfs/blob/c5073360007d28385a33426a42ac3e4ec504c5a3/weed/command/server.go#L337-L356) routes local gRPC through Unix sockets there and [S3 waits for the filer](https://github.com/seaweedfs/seaweedfs/blob/c5073360007d28385a33426a42ac3e4ec504c5a3/weed/command/s3.go#L298-L324). The second run's bounded output did not include raw container logs, so this is a source-backed causal diagnosis, not a quoted runtime error. [PR59](https://github.com/CleMeY15/auto-world/pull/59) added a private 16 MiB `/tmp` tmpfs and separately waits for [filer `/readyz`](https://github.com/seaweedfs/seaweedfs/blob/c5073360007d28385a33426a42ac3e4ec504c5a3/weed/server/filer_server.go#L239-L242), preserving strict S3 HTTP 403 and all isolation controls. It merged at `2097e2042809625b1736ea88bbf39657495c9df6` from final head `3506db245c3ff201fa38552c5af0e2a7578ee650`; [exact-head CI 35946876972](https://github.com/CleMeY15/auto-world/actions/runs/35946876972) and [protected-main CI 35947042385](https://github.com/CleMeY15/auto-world/actions/runs/35947042385) passed all gates. Independent code review found no blocking issue; formal review was limited by unavailable LSP diagnostics.

The third [native runtime run 35947192944](https://github.com/CleMeY15/auto-world/actions/runs/35947192944), run 3 attempt 1 on that exact protected main SHA, **SUCCEEDED** with `SEAWEED_LOCAL_RUNTIME_CANDIDATE_RECEIPT_V1/VERIFIED/DIAGNOSTIC_ONLY/NOT_AUTHORIZED`. Its newly reconstructed image ID was `sha256:0ae3c373e181d730a840465dcbe782b3e12b7c7b3377ff4d5ea58cdd480af04f`. The runtime proof records UID/GID `1000:1000`, cluster status 200 on master port 9333, filer and S3 `/readyz` 200, unsigned S3 `GET /` refused with exact HTTP 403, ports 8181/9101 closed, both Rust helpers absent and direct execution rejected, and bounded shutdown. Its receipt reports `publication`, `vulnerabilityAudit` and `admission` as `NOT_ATTEMPTED`; the separate final cleanup reported `CLEANED` and retained no image or archive. The two earlier native failures remain failures. This diagnostic proves only this disposable image's bounded local behavior; signed S3 requests, persistence/restart/restore, a fresh exact-subject audit and private admission still require separate evidence.

[PR60](https://github.com/CleMeY15/auto-world/pull/60) recorded that exact boundary at protected main `4bf20c502a6774241ba48016144adfd92ece545e`, with [exact-head CI 35947764259](https://github.com/CleMeY15/auto-world/actions/runs/35947764259) and [main CI 35947919072](https://github.com/CleMeY15/auto-world/actions/runs/35947919072) passing. The next one-time diagnostic reconstructs another disposable candidate on reviewed main and probes signed S3 behavior inside the same network-isolated container. It requires a signed create/conditional PUT/GET and exact readback digest in `aw-raw`, rejects a wrong credential and a forbidden bucket, then checks sequential conditional writes without accepting an overwrite. One pair of background conditional PUTs must produce one winning payload and matching readback; this observation alone does not establish request overlap or atomic conflict handling under contention. That ADR-0008 concurrency gate remains **NOT VERIFIED**. The public receipt can expand only after all probes, bounded shutdown and owned cleanup succeed. The code and synthetic configuration introduce no registry write, host-published port, retained volume or admission. Until the fourth native run succeeds, these signed-operation checks are **NOT VERIFIED**; the third run's narrower receipt remains valid only for its own disposed image.

Rollback reverts the diagnostic code and one-time workflow. It changes no retained image, registry object, persistent volume or production service.
